<% provide(:title, "Attendance Record ") %>

<div class="conatiner">
  <div class="row">
    <div class="col-sm">
      <% if admin? %>
        <%= render 'attendances/attendance_table' %>
      <% end %>
    </div>
  </div>
</div>
