<% content_for :bread_crumbs do %>
  <div class="row d-flex p-1">
      <p class="m-0 align-middle"><%= link_to "#{@project.department.name.titleize}", department_path(@project.department), class: 
    'text-prim no-decoration' %> > <%= @project.name.titleize %> > Edit Project</p>
      <div class="m-0 pl-2">
        <%= link_to department_projects_path(@project.department), :class => "btn btn-regular btn-circle btn-md" do %>
          <i class="fas fa-arrow-left"></i>
        <% end %>
      </div>
  </div>
<% end %>

<%= render 'form' %>
