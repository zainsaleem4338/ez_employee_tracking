<% provide(:title, "All Departments") %>
<p id="notice"><%= notice %></p>

<% if @departments.length > 0 %>
<table class="table">
  <thead class="thead-dark">
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @departments.each do |department| %>
      <tr>
        <td><%= department.name %></td>
        <td><%= department.description %></td>
        <td><%= link_to 'Show', department, class: "btn btn-regular btn-secondary" %>
        <%= link_to 'Edit', edit_department_path(department), class: "btn btn-outline-secondary" %>
        <%= link_to 'Destroy', department, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-outline-danger" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<% end %>

<br>

<%= link_to 'New Department', new_department_path, class: "btn btn-regular btn-primary"%>
