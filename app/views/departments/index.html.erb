<p id="notice"><%= notice %></p>

<h4 class="h4">All Departments</h4>

<% if !can? :create, Department%>
    <%= link_to "All Departments", departments_path, remote: true, class: "btn btn-secondary active"%>
    <%= link_to "My Departments", departments_path(checked: true), remote: true, class: "btn btn-secondary active" %>
<% end %>

<div id="departments_grid" >
  <%= render partial: 'departmentgrid', locals: {departments: @departments}  %>
</div>

<br>

<%= link_to 'New Department', new_department_path, class: "btn btn-primary"%>