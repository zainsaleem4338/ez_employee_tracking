<h1>All Events of <%= current_company.name %></h1>

<ul class="list-group">
  <% @events.each do |event| %>
    <li class="list-group-item list-group-item-info">
      <ul>
        <li><%= event.title %></li>
        <li><%= event.description %></li>
        <li><%= event.event_date %></li> 
        <li><b><%= link_to "Edit this event", edit_events_path(event) %></b></li>
        <li><b><%= link_to "Delete this event", destroy_events_path(event), method: :delete, data: { confirm: "Are you sure you want to delete this event?" } %></b></li> 
      </ul>
    </li>
  <% end %>
</ul>